{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "launch gdb",
            "stopAtEntry": true,
            "target": "${fileDirname}/build/program.exe",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "gcc - build current file"
        }
    ]
}